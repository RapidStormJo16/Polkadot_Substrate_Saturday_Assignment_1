1)

fn main() {
    // Use as many approaches as you can to make it work
    let x = String::from("hello, world");
    let y = x.clone();
    println!("{},{}",x,y);
}

2)

fn main() {
    // Use as many approaches as you can to make it work
    let x = "hello, world";
    let y = x.clone();
    println!("{},{}",x,y);
}